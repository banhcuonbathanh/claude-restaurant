# Excalidraw-Map Tracker

> Progress for `/excalidraw-map <page-folder>` — the multi-panel doc knowledge map.
> One row per page doc-set under `docs/system/08_pages/`. Update this after each page.
> Model / reference build: **customer_menu** (15 panels, 1661 elements — built before
> the Lens Mockups panel was dropped; new maps target the 14-panel catalog).

**Status legend:** ✅ done · 🔄 in progress · ⬜ not started · ⚠️ blocked (thin/missing docs)

**Progress: 12 / 29 done**

---

## Customer (7 / 10)

| Status | Page | Run | Findings |
|---|---|---|---|
| ✅ | customer_menu | — (reference build) | 15 panels, 1661 elements. Source for `excalib.py` + PANEL_CATALOG. |
| ✅ | customer_checkout | `/excalidraw-map customer_checkout` | 13 panels, 284 elements. Skipped catalog #2 Cross-Component (N/A — single widget, doc-stated). 3 code bugs surfaced (payment_method dead · TABLE_HAS_ACTIVE_ORDER dead branch · online 403). |
| ✅ | customer_combo_detail | `/excalidraw-map customer_combo_detail` | 12 panels, 362 elements. Skipped catalog #2 Cross-Component (no multi-widget store — only local `qty` useState; doc-set has no `_crosscomponent_dataflow.md` BY DESIGN, stated in SCENARIO §A) + #12 Realtime (no SSE/WS — `_crosspage §1` = "None"). Read-only-on-BE leaf: 2 public cached catalog GETs in, 1 client-side cart write out, ZERO order write on this page. Carried doc-set's flags/bugs (COMBO_BUGS.md): 🟡 Bug 1 unavailable-combo UI unreachable (`ListCombosAvailable` filters `is_available=1` → not-found shown instead of "Hết hàng" badge/disabled CTA) · 🟠 Bug 2 unavailable sub-product → raw UUID as item name (`productMap` miss, `page.tsx:45`). + flags: no `GET /combos/:id` (over-fetch whole catalog) · FE-side name/price join (2 independently-stale 5-min queries) · error & not-found indistinguishable · silent `products` failure · ⚠ drift key `'cart-config-v3'` vs persist `version:5` · combo cart item is session-only (lost on F5 before submit). |
| ✅ | customer_favourites | `/excalidraw-map customer_favourites` | 12 panels, 357 elements. Skipped catalog #4 Object Model (`customer_favourites.md` has NO §Object Model heading — FavouriteItem→Resolved→CartItem transform covered by Panels 9/10/14) + #12 Realtime Fan-out (suite has NO SSE/WS, issues ZERO BE writes — pull + 5-min TTL only; folded into Panels 5/13). Panel 11 reframed "Storage Row-Level" — there is NO `favourites`/`favourite_sets` DB table or migration; persistence is 100% localStorage `'favourites'`, DB only serves the shared cached catalog reads. BE-read-only suite: 2 public cached GETs (`/products`+`/combos`) in, client-store writes out (favourites persisted + cart session). No `_BUGS.md` — trace found no FE/BE disagreement. Carried doc-set flags (not this-page bugs): whole suite BE-read-only (no account save — lost on storage clear/new device) · availability inferred not queried (~10min stale before prune) · `GET /products` ignores all query params · combo names resolved vs products → 'Món không rõ tên'/raw id fallback · toggleFav drops menu qty/toppings (always {qty:1,toppingIds:[]}) · duplicate resolve logic ×3 pages · 🔴 empty=loading=error indistinguishable on list (silent network failure) · 'Add all' acts on resolvedItems not filteredItems · addSet snapshots (frozen, not live) · stale useEffect dep array booleans-only (won't re-run mid-session). |
| ✅ | customer_order_detail | `/excalidraw-map customer_order_detail` | 13 panels, 483 elements. doc-set MISSING `customer_order_detail_crosscomponent_dataflow.md` → Panel 2 traced from SCENARIO §A + crosspage §6. Skipped catalog #4 Object Model (page `.md` has no §Object Model — covered by Panels 9+11). 2 code bugs + drift surfaced: 🟠 Bug 1 qty edit dead (`item_updated` no case + dead `invalidateQueries`) · 🟡 Bug 2 `item_cancelled` no case · ⚠ `items_added` dropped · ⚠ SSE no ownership check · 🔴 30% cancel-rule DRIFT vs owner target. |
| ✅ | customer_order_list | `/excalidraw-map customer_order_list` | 13 panels, 518 elements. Skipped catalog #4 Object Model (page `.md` has NO §Object Model — covered by Panels 9+11). ⚠️ doc-set MISSING `customer_order_list_crosscomponent_dataflow.md` → Panel 2 traced from SCENARIO §A + crosspage §4 instead. Surfaced 4 live bugs (already in TRACKING_BUGS.md): 🔴 404 wedges overlay spinner (`isNotFound` never read) · 🟠 list cards never refetch · 🟠 `item_cancelled` published-not-consumed · 🟡 SSE handler has no ownership check. |
| ⬜ | customer_product_detail | `/excalidraw-map customer_product_detail` | |
| ⬜ | customer_profile | `/excalidraw-map customer_profile` | |
| ⬜ | customer_table_qr | `/excalidraw-map customer_table_qr` | |
| ✅ | customer_tracking | `/excalidraw-map customer_tracking` | 14 panels, 463 elements. Panels 4/11/14 adapted to READ semantics (page is read-only, no writes). Panel 5 (Cross-Page) sourced from _crosscomponent §5 + SCENARIO §B — no `customer_tracking_crosspage_dataflow.md` (absent **by design**: read-only consumer hands nothing off). 4 live code bugs surfaced: 🔴 status badge dead (`order.status` vs `order_status_changed`) · 🟠 `item_progress` not consumed · 🟡 position/ETA FE-computed (BE sends 0) · 🟡 dead outputs (`tableStatuses`/`reconnect()`/`showBannerAfter`). |

## Staff (0 / 5)

| Status | Page | Run | Findings |
|---|---|---|---|
| ⬜ | staff_login | `/excalidraw-map staff_login` | |
| ⬜ | staff_register | `/excalidraw-map staff_register` | |
| ⬜ | staff_kds | `/excalidraw-map staff_kds` | |
| ⬜ | staff_pos | `/excalidraw-map staff_pos` | |
| ⬜ | staff_cashier_payment | `/excalidraw-map staff_cashier_payment` | |

## Admin (5 / 12)

| Status | Page | Run | Findings |
|---|---|---|---|
| ✅ | admin_overview | `/excalidraw-map admin_overview` | 14 panels, 643 elements. Full doc-set (SCENARIO file named `SCENARIO_OVERVIEW_FLOOR.md`). All 14 built — read-write live dashboard. Panel 4 (Object Model) sourced from traced `order.ts` types + be.md (page `.md` has NO §Object Model section). Surfaced the docs' 9 flags: ⚠ `delivered→cancelled` Huỷ button always 409s · optimistic update not rolled back on error · ⚠ WS `/ws/orders-live` has NO role gate (customer JWT can open live floor) · `POST /payments` ignores FE `amount` · WS dead cases `order_updated`/`order_completed` · `orders/history` returns `items:[]` · Zone B feed mislabelled `/orders` (is `/orders/live`). |
| ✅ | admin_summary | `/excalidraw-map admin_summary` | 13 panels, 559 elements. Skipped catalog #12 Realtime Fan-out (page has NO SSE/WS — refetch-only by design; folded into Panel 5). ⚠️ doc-set MISSING `admin_summary_crosscomponent_dataflow.md` → Panel 2 traced from `SCENARIO §A` + `admin_summary.md §Zones`. Panel 4 (Object Model) adapted: page owns NO model (read-aggregation + 1 write) — drew the consumed Order/Ingredient pipelines. 10 source-traced flags carried (none are this-page bugs): static "Khách hôm nay" label · "(delivered)" under-describes delivered+paid · 🔴/🟡 split FE-only · top-dishes pct = % of top-N not period · active_tables range-agnostic · `<a>` not next/link · stock-in NOT transactional · adjustment ADDS not sets · limit>50 resets to 5 · no isError UI (skeleton stuck). |
| ⬜ | admin_products | `/excalidraw-map admin_products` | |
| ⬜ | admin_categories | `/excalidraw-map admin_categories` | |
| ⬜ | admin_combos | `/excalidraw-map admin_combos` | |
| ⬜ | admin_toppings | `/excalidraw-map admin_toppings` | |
| ⬜ | admin_ingredients | `/excalidraw-map admin_ingredients` | |
| ✅ | admin_staff | `/excalidraw-map admin_staff` | 13 panels, 583 elements. Skipped catalog #4 Object Model (`admin_staff.md` has NO §Object Model — `_be.md` says staff has no OBJECT_MODEL doc; folded field-mapping into Panel 11 DB rows). ⚠️ doc-set MISSING `admin_staff_crosscomponent_dataflow.md` — intentional (no shared Zustand store; page.tsx prop-drills, stated in `SCENARIO §A`) → Panel 2 traced from `admin_staff.md §Zones` + `SCENARIO §A`. Panel 12 (Realtime) re-themed "Write Fan-out" — staff has NO SSE/WS (pull-only). No `_BUGS.md`: FE/BE agree on every route. Source-traced flags carried (not this-page bugs): dead self-service guard · unused list filter params · 100-row client cap · no admin minted here · self-status fully blocked · soft-delete keeps refresh_tokens · bad FK→500 · `performance_score`=0 hardcoded stub (no such column) · drawer no error-branch · loading≠empty≠no-match identical surfaces. |
| ✅ | admin_marketing | `/excalidraw-map admin_marketing` | 11 panels, 519 elements. Skipped catalog #5 Cross-Page (read-only — no localStorage write / no Zustand other pages read), #11 DB Row-Level (NO marketing table / no SQL / no migration), #12 Realtime (NO SSE/WS — pull-only, be Flag 4). Both `_crosscomponent_dataflow.md` + `_crosspage_dataflow.md` intentionally absent (N/A — single-query prop-drill, pure read; stated in `SCENARIO §A`/§B) → Panel 2 traced from `SCENARIO §A`. ⚠️ doc-vs-code drift: `.md` ASCII wireframe shows spent 32.000k/64%, traced BE = 18.500k/37% (`.md` figures illustrative — code wins). Whole endpoint is a hardcoded pre-launch STUB (no service/repo/SQL/Redis); from/to accepted but ignored; 2 action buttons are toasts. No `_BUGS.md` — only intentional-stub behaviour. |
| ✅ | admin_task_board | `/excalidraw-map admin_task_board` | 13 panels, 492 elements. Skipped catalog #12 Realtime Fan-out (task domain has NO SSE/WS/localStorage — explicit in `_crosspage §Multi-device sync`; "polling-only" folded into Panel 5, Panel 12 reused for Failure/Edge Map). Panel 4 (Object Model) sourced from traced `types/task.ts` + `_be.md` TaskDTO + SCENARIO column table (`admin_task_board.md` has NO §Object Model). SCENARIO file named `SCENARIO_ASSIGN_TASK.md`. Page has NO Zustand → Panel 9 client layer = `page.tsx` useState. 4 code bugs (`TASK_BOARD_BUGS.md`): 🔴 status write-once `pending` → 3/4 KPIs + completion% + quality + overdue PERMANENTLY 0 (also breaks A9 todo-list) · 🟠 `qualityScore`=completionRate/20 fabricated · 🟡 invalid `assigned_to`→500 not 4xx (FK 1452 → ErrInternalError; dead ErrNoRows branch) · 🟡 status FilterBar dead (only `overdue` wired, always false). + Flag5 `description` stored-never-shown · Flag6 required `dueTime` not stored as own field · invalidate uses `task.dueDate` not `filters.date`. |
| ⬜ | admin_todo_list | `/excalidraw-map admin_todo_list` | |
| ⬜ | admin_training | `/excalidraw-map admin_training` | |

## Public (0 / 1)

| Status | Page | Run | Findings |
|---|---|---|---|
| ⬜ | public_landing | `/excalidraw-map public_landing` | |

---

## Findings log
> Note doc-vs-code drift, missing doc-set files, or panels skipped — discovered while building. One line per finding.

- _(none yet — reference page customer_menu was hand-built before the skill existed)_
- **admin_task_board** — doc-set complete (6 gold-standard files + extra `TASK_BOARD_BUGS.md`; SCENARIO named `SCENARIO_ASSIGN_TASK.md`). Skipped catalog #12 Realtime Fan-out — the task domain has NO SSE, NO WebSocket, NO localStorage (explicit in `_crosspage_dataflow.md §Multi-device sync`: each browser polls independently, max lag ~60s); "polling-only" folded into Panel 5 and slot 12 reused for the Failure/Edge map. Panel 4 (Object Model) traced from `types/task.ts` + `_be.md` TaskDTO + SCENARIO column table (`admin_task_board.md` has no §Object Model). Page uses NO Zustand → Panel 9/10 client layer drawn as `page.tsx` useState coordinator. 4 code bugs (already in `TASK_BOARD_BUGS.md`, none stale-doc): 🔴 task `status` write-once `pending` (no UPDATE query / no job / no route advances it) → 3 of 4 KPIs, completion%, quality stars, overdue highlight permanently 0/false — cross-cuts A9 todo-list (same table) · 🟠 `qualityScore` fabricated as `completionRate/20` (no stored quality column) · 🟡 invalid `assigned_to` → FK 1452 mapped to ErrInternalError → 500 not 4xx (dead `sql.ErrNoRows` branch) · 🟡 status FilterBar dead (page.tsx honours only `overdue`, which is always false per Bug 1). Plus: `description` stored but never rendered · required `dueTime` folded into `due_at` only (column shows '—') · `onSuccess` invalidates `task.dueDate` not `filters.date` (KPIs may not refresh cross-date).
- **customer_checkout** — `customer_checkout_crosscomponent_dataflow.md` is intentionally absent (N/A: one widget + a single cart read, no multi-writer shared store — stated in `_crosspage_dataflow.md:37` + `SCENARIO §A`). Cross-Component panel skipped; an N/A note folded into Panel 1. Doc-set otherwise rich (incl. extra `CHECKOUT_BUGS.md` with 3 code-level bugs).
- **customer_tracking** — `customer_tracking_crosspage_dataflow.md` intentionally absent (read-only consumer hands nothing off; stated in `_crosscomponent §5`). Panel 5 sourced from `_crosscomponent §5` + `SCENARIO §B` durability matrix. Panels 4/11/14 adapted to READ semantics (no writes). Doc-set rich incl. extra `TRACKING_BUGS.md`. 4 live code bugs (not stale docs): 🔴 status badge never updates from SSE (`order.status` vs published `order_status_changed`) · 🟠 `item_progress` published but no hook case · 🟡 queue position/ETA FE-computed while BE sends 0 placeholders · 🟡 dead outputs `tableStatuses`/`reconnect()`/`RECONNECT.showBannerAfter`.
- **admin_overview** — doc-set complete (6 files; SCENARIO is `SCENARIO_OVERVIEW_FLOOR.md`, not `SCENARIO_ADMIN_OVERVIEW.md`). No panels skipped — read-write live dashboard supports all 14. Panel 4 (Object Model) traced from `order.ts`/`admin.api.ts` type defs + `_be.md` read/write pipelines because `admin_overview.md` has no §Object Model section (only Zones + Business Logic). No stale-doc drift found; the docs already enumerate the code flags. Live code flags carried into Panel 8/13 (all source-traced, not stale docs): ⚠ `delivered→cancelled` Huỷ button on delivered orders → guaranteed 409 hidden by generic toast · optimistic status write never rolled back on error (heals via WS echo / 15s refetch) · 🔴 WS `/ws/orders-live` has no `authMW` role gate — any parseable JWT incl. `customer` can open the live floor (SSE is correctly manager+) · `POST /payments` DTO ignores FE `amount` (server uses `order.total_amount`) · `useOverviewWS` dead switch cases `order_updated`/`order_completed` (BE never emits) · `ListTodayHistory` returns `items:[]` · FE sibling mislabels Zone B source `GET /orders` (real = `/orders/live`).
- **admin_summary** — doc-set MISSING `admin_summary_crosscomponent_dataflow.md` (other 5 of the gold-standard 6 present). Panel 2 (Cross-Component) traced from `SCENARIO_SUMMARY_REVIEW.md §A` (range useState → prop fan-out to 3 children; StockAlertList range-independent; no store) + `admin_summary.md §Zones`. Skipped catalog #12 Realtime Fan-out — page has NO SSE/WS (explicitly refetch-only, `_crosspage §5` + `SCENARIO §D`); the "no realtime / next-refetch" fact folded into Panel 5. Panel 4 (Object Model) adapted: `admin_summary.md §Object Model` states the page owns NO model — drew the consumed READ (Order/OrderItem analytics SQL) + WRITE (Ingredient/StockMovement stock-in) pipelines instead of entity fields. No stale-doc drift found — the docs already enumerate all flags; carried into Panels 8/13. These are behavioural notes, NOT this-page bugs (docs note `SUMMARY_BUGS.md` was deliberately not created): static "Khách hôm nay" label for week/month · "(delivered)" caption under-describes BE `delivered+paid` · 🔴/🟡 low-stock split is FE-only (BE returns ≤ min*1.2) · top-dishes `pct` = % of returned top-N not period share · `active_tables` range-agnostic live count · "Xem toàn bộ kho" uses raw `<a>` (full nav) · stock-in INSERT+UPDATE not transactional · `type:'adjustment'` ADDS not sets absolute · `limit>50` resets to 5 · no `isError` UI in any section (failed fetch → skeleton pulses forever, KPI shows 0).
- **admin_staff** — doc-set MISSING `admin_staff_crosscomponent_dataflow.md` (the other 5 of the gold-standard 6 present). This is **intentional, not an oversight**: the page has no shared Zustand store — `page.tsx` owns all state in `useState` and prop-drills to children (stated verbatim in `SCENARIO_STAFF_MANAGE.md §A`: "that's why there's no `_crosscomponent_dataflow.md` for this page"). Panel 2 (Cross-Component) traced from `admin_staff.md §Zones` + `SCENARIO §A` (page-as-hub, one TanStack query, client-side `filtered` memo + 10/page slice). Skipped catalog #4 Object Model — `admin_staff.md` has no §Object Model section and `_be.md` notes staff has no dedicated `OBJECT_MODEL` doc (fields owned by `DB_SCHEMA.md staff`); field-mapping folded into Panel 11 (DB Row-Level View). Panel 12 (Realtime Fan-out) re-themed "Write Fan-out (pull-only, NO SSE)" — staff management has no SSE/WS; the only near-live effect is the target's auth-cache lockout (`Del auth:staff:<id>` → middleware re-read → 401). No stale-doc drift; the docs already enumerate all flags (carried into Panels 8/13) and explicitly note no `_BUGS.md` was created because FE and BE agree on every route/shape/guard — the only mismatches are dead defensive code (be Flags 1/5) and the `performance_score=0` stub (be Flag 8, no backing column).
- **admin_marketing** — doc-set has 4 of the gold-standard 6: `_crosscomponent_dataflow.md` AND `_crosspage_dataflow.md` are **both intentionally absent (N/A)**, stated verbatim in `SCENARIO_MARKETING.md §A` ("single data source → prop-drill, no Zustand hub") and §B ("pure read, writes nothing cross-page"). Panel 2 (Cross-Component) traced from `SCENARIO §A` (one `useMarketingSpend` → props to 5 leaves; `CampaignTimeline` static, outside the query). Skipped 3 catalog panels — #5 Cross-Page (read-only, no localStorage/Zustand handoff), #11 DB Row-Level (no `marketing` table, no SQL, no migration — endpoint is a hardcoded handler), #12 Realtime Fan-out (no SSE/WS, pull-only, be Flag 4). Panel 4 (Object Model) adapted to a READ-only FE⇄BE pipeline with an explicit empty DB layer. ⚠️ **doc-vs-code drift surfaced while building**: `admin_marketing.md` ASCII wireframe shows spent **32.000k / 64%** but the traced BE constants (be.md + SCENARIO) are **18.500k / 37%** — the `.md` figures are illustrative/older; code wins (carried into Panel 8 as flag DOC). No `_BUGS.md`: the whole page is an intentional pre-launch stub (1 GET, no service/repo/SQL/Redis; from/to accepted but never applied — Flag 1; "Xuất báo cáo"/"+ Nhập chi tiêu" are placeholder toasts — Flag 3), so doc edits, not code, are the correct response.
- **customer_order_list** — doc-set MISSING the dedicated `customer_order_list_crosscomponent_dataflow.md` (the other 5 of the gold-standard 6 present, + extra `TRACKING_BUGS.md`). Panel 2 (Cross-Component) traced from `SCENARIO §A` (the `useOrderSSE → order → widgets` hub) + `_crosspage §4` instead. Panel 4 (Object Model) skipped — page `.md` has no §Object Model section (links out to `OBJECT_MODEL_ORDER.md`); entity shape covered by Panel 9 (live objects) + Panel 11 (DB rows). 4 live code bugs (already logged in `TRACKING_BUGS.md`, none stale-doc): 🔴 404/foreign order wedges overlay spinner (`isNotFound` returned but `OrderDetailSheet` never destructures it) · 🟠 list cards read cache once, never refetch → stale until overlay opened · 🟠 `item_cancelled` published (`order_service.go:642`) but no `useOrderSSE` case → cancel not live · 🟡 `StreamOrder` SSE behind `authMW` only, no `table_id` ownership check (REST paths do enforce it). Shared `OrderDetailSheet`/`useOrderSSE` pair ⇒ bugs also apply to `customer_order_detail` (C10).
- **customer_order_detail** — 13 panels, 483 elements. doc-set has 5 of the gold-standard 6 + extra `ORDER_DETAIL_BUGS.md`; **MISSING `customer_order_detail_crosscomponent_dataflow.md`** → Panel 2 (Cross-Component) traced from `SCENARIO §A` (the `useOrderSSE useState → order → memos → DishRow/SummaryTable/MoneyCard` hub; NO TanStack query for the order) + `_crosspage §6` (SSE event table). Skipped catalog #4 Object Model — `customer_order_detail.md` has no §Object Model section (links out to `OBJECT_MODEL_ORDER.md`); entity shapes covered by Panel 9 (live objects) + Panel 11 (DB rows) — same skip as C9. This is the richest customer page (read + 3 write paths incl. the page-unique `PATCH /orders/items/:id/quantity` #5). 2 code bugs (already in `ORDER_DETAIL_BUGS.md`, none stale-doc): 🟠 Bug 1 quantity edit never reflects live — BE publishes `item_updated` (`order_service.go:696`) but `useOrderSSE` has no case AND `onSuccess invalidateQueries(['order',id])` is a no-op (no such `useQuery`) → traced end-to-end in Panel 14 as a broken round-trip · 🟡 Bug 2 `item_cancelled` (`order_service.go:642`) no case → cancelled row stays. Same root drops `items_added` too (sibling-device adds invisible). Plus carried into Panels 8/13: ⚠ `StreamOrder` SSE behind `authMW` only, no `table_id` ownership check (REST paths enforce it) · ⚠ no status replay on (re)connect · ⚠ dead `order_init` case (no publisher) · 🔴 **DRIFT** — both FE gate (`canCancelOrder progress<30`) and BE `ErrCancelThreshold` still enforce the 30% cancel rule vs owner target (2026-06-12) "cancel any time before payment". ⚠️ git checkpoint was BLOCKED (settings deny) so no pre-build commit was made.

## How to refresh this list
```bash
# pages that HAVE a full doc-set (one *_be.md = ready to map):
find docs/system/08_pages -name "*_be.md" | sort
# pages that already HAVE a map built:
find docs/system/08_pages -name "*.excalidraw" ! -name "* copy*" ! -name "*.bak" | sort
```
