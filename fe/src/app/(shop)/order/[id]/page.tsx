// Phase 5.3 — Order tracking page with SSE
// Ref: prompts/phase5_3_checkout_sse.md
export default function OrderPage({ params }: { params: { id: string } }) {
  return <div>Order {params.id} — TODO Phase 5.3</div>
}
